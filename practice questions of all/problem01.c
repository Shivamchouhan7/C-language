#include <stdio.h>

int main(){int length, breadth;
    printf("enter the length :\n ");
    scanf("%d",&length);
    printf("enter the breadth : ");
    scanf("%d",&breadth);
    printf("the area of rectangle is %d",length*breadth);
    return 0;
}
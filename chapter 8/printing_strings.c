#include <stdio.h>
//PRINTING STRINGS WITHOUT USING LOOP
int main(){
    // Initializing a string using double quotes
    char string[]="Hello";
    /* for (int i = 0; i <3; i++)
       {
       printf(" character is %c\n",string1[i]); 
       } */
    // Directly printing the string using the format specifier for strings
    printf("%s \n", string); 
    return 0;
}